<h2 class="page-header"><%= users.email %></h2>
<div class="btn-row">
  
<a class="btn btn-primary" href="/users/<%= users.id %>/edit">Editar</a>
<a class="btn btn-primary" href="/users/dashboard">Voltar</a>
<%- include('../partials/deleteForm',
            { url: `/users/${users.id}` }) %>
          </div>
